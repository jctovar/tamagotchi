# Generate App Icons

Generate launcher icons for all platforms.

## Instructions

Run the flutter_launcher_icons package to generate icons:

```bash
flutter pub run flutter_launcher_icons
```

This will generate icons based on the configuration in pubspec.yaml.

After generation, list the platforms that were updated.
