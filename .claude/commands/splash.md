# Generate Splash Screens

Generate native splash screens for all platforms.

## Instructions

Run the flutter_native_splash package:

```bash
flutter pub run flutter_native_splash:create
```

This will generate splash screens based on the configuration in pubspec.yaml.

After generation, list the platforms that were updated.
