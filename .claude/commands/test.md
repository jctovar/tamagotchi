# Run Flutter Tests

Execute the Flutter test suite.

## Arguments
- `$ARGUMENTS` - Optional: specific test file or directory

## Instructions

Run Flutter tests:

```bash
flutter test $ARGUMENTS
```

If tests fail:
1. Analyze the failure output
2. Identify the failing tests
3. Suggest potential fixes

If all tests pass, report the summary.
