# Fix Flutter Issues

Automatically fix common Flutter/Dart issues.

## Instructions

Run dart fix to apply automated fixes:

```bash
dart fix --apply
```

Then run analysis to verify:

```bash
flutter analyze
```

Report what was fixed and any remaining issues.
